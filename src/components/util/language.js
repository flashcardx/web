export default function(code){
     switch (code) {
            case "en": return "English";
            case "es": return "Español";
            default:
                return "not available";
        }
}